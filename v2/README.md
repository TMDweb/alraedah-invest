# Alraedah Invest, Landing Page (polish-pass-v1 preview)

This is a snapshot of the `polish-pass-v1` branch, served alongside the main landing page at the same GitHub Pages URL so the team can compare both versions in a browser.

- Main version: `https://tmdweb.github.io/alraedah-invest/`
- This version (polish-pass-v1): `https://tmdweb.github.io/alraedah-invest/v2/`

Both pages are served from the same repository. `main/` serves `/`, and `main/v2/` serves `/v2/`, which is a file-copy of the polish-pass-v1 branch.

See PR #1 on the repo for the full diff between the two.
